# Kong config
_format_version: "3.0"

services:
  # Joke API service
  - name: joke-service
    url: http://10.0.1.10:4000 
    routes:
      # Root route
      - name: joke-root
        paths:
          - /
        methods:
          - GET
        strip_path: false

      # Types route
      - name: joke-types
        paths:
          - /types
        methods:
          - GET
        strip_path: false

      # Jokes query route
      - name: joke-query
        paths:
          - /jokes
        methods:
          - GET
        strip_path: false

      # Joke by type route
      - name: joke-by-type
        paths:
          - /joke
        methods:
          - GET
        strip_path: false

  # Submit app service
  - name: submit-service
    url: http://10.0.1.10:4200 
    routes:
      # Submit HTML route
      - name: submit-html
        paths:
          - /submit-form
        methods:
          - GET
        strip_path: true

      # Submit POST route
      - name: submit-post
        paths:
          - /submit
        methods:
          - POST
        strip_path: false

      # Submit docs route
      - name: submit-docs
        paths:
          - /docs
        methods:
          - GET
        strip_path: false

  # Moderate app service
  - name: moderate-service
    url: http://10.0.1.10:4300
    routes:
      # Moderate HTML route
      - name: moderate-html
        paths:
          - /moderate-form
        methods:
          - GET
        strip_path: true

      # Moderate GET route
      - name: moderate-get
        paths:
          - /moderate
        methods:
          - GET
        strip_path: false

      # Moderated POST route
      - name: moderated-post
        paths:
          - /moderated
        methods:
          - POST
        strip_path: false

      # Moderate types route
      - name: moderate-types
        paths:
          - /moderate/types
        methods:
          - GET
        strip_path: true


# Plugins
plugins:
  # Rate limiting plugin
  - name: rate-limiting
    route: joke-by-type
    config:
      minute: 10
      policy: local
